# NetBox MCP Server Dependencies
# 
# Core dependencies for production deployment
pynetbox==7.5.0
requests>=2.28.0
pydantic>=2.0.0
python-dotenv>=1.0.0
pyyaml>=6.0
tomli>=2.0.0
typing-extensions>=4.0.0
cachetools>=5.0.0
psutil>=5.9.0

# MCP (Model Context Protocol) - requires 1.9+ for streamable-http support
mcp[cli]>=1.9.0

# Web server dependencies
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
starlette>=0.27.0
httpx>=0.24.0
httpx-sse>=0.3.0

# Install the project itself
-e .
